Pure ijji Gunz source with website

includes database extensions for shop and such
also might need to edit the stored procedure os spBringAccountItem2 to spBringAccountItem and spBringAccountItem to spBringAccountItemOld

totally clean source with clean database

works with xampp server and php7 so far. if updates for functions in php were made, might need to fix the errors.